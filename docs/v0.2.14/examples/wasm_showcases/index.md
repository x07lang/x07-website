# WASM showcase apps

The `x07_atlas`, `x07_studio`, and `x07_field_notes` showcase mirrors were removed in the [2026-06 refocus](../../roadmap.md): they depended on the archived `x07-web-ui` and `x07-device-host` lanes.

The canonical runnable WASM projects live in `x07-wasm-backend/examples/`.
