# x07import (C) by example (companion example)

This directory is a companion to:

- `docs/x07import/porting-by-example.md`

Generate (or re-generate) the imported module:

```bash
bash scripts/gen.sh
```

Run tests:

```bash
x07 test --manifest tests/tests.json
```

